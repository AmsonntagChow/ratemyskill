interface:
  display_name: "RateMySkill"
  short_description: "Evidence-based audits for Agent Skills"
  default_prompt: "Use $ratemyskill to audit this Agent Skill, find release blockers, and give me the three fastest fixes."
policy:
  allow_implicit_invocation: true
