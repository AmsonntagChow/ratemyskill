## Behavior change

What user-visible or agent-visible behavior changes?

## Evidence

- Trigger prompt 1:
- Trigger prompt 2:
- Trigger prompt 3:
- Near-miss prompt, if discovery changes:
- Before observation or transcript:
- After observation or transcript:

## Evaluation

- [ ] I added or updated trigger evals when discovery behavior changed.
- [ ] I added or updated execution evals when review behavior changed.
- [ ] I removed assertions that both with-skill and without-skill arms always pass.
- [ ] I ran `python3 scripts/validate_repo.py`.
- [ ] I ran `python3 -m unittest discover -s tests -v`.
- [ ] I did not add telemetry, hidden network access, or tool auto-authorization.
- [ ] My commits include a DCO `Signed-off-by` line.

## Description surface

Did `skills/ratemyskill/SKILL.md` frontmatter `description` change? If yes, explain the intended positive and near-miss behavior.
