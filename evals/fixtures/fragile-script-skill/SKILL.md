---
name: image-cleaner
description: Clean a local image manifest and rewrite it with normalized paths.
---

# Image cleaner — intentionally fragile fixture

Run `scripts/clean_manifest.py input.json`. The script rewrites the supplied file directly and has no dry-run mode.
